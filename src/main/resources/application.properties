spring.application.name=imobiliaria

# Configurações de conexão banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/imobiliaria?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Configurações JPA/Hibernate
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# # Configurações do servidor
# server.port=8080
# server.servlet.context-path=/api
# # Configurações de logging
# logging.level.org.springframework=INFO
# logging.level.com.example.demo=DEBUG
# logging.file.name=logs/imobiliaria.log
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
# logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
# # Configurações de cache (se necessário)
# spring.cache.type=simple
# # Configurações de segurança (se necessário)
# spring.security.user.name=admin
# spring.security.user.password=admin123
# # Configurações de internacionalização (i18n)
# spring.messages.basename=messages
# spring.messages.encoding=UTF-8
# spring.messages.fallback-to-system-locale=false
# # Configurações de CORS (se necessário)
# #spring.web.cors.allowed-origins=http://localhost:3000
# #spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# #spring.web.cors.allowed-headers=*
# #spring.web.cors.allow-credentials=true
# # Configurações de upload de arquivos (se necessário)
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB
# # Configurações de email (se necessário)
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=user@example.com
# spring.mail.password=secret
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true  
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.properties.mail.smtp.writetimeout=5000
# # Configurações de Swagger (se necessário)
# springdoc.api-docs.path=/v3/api-docs
# springdoc.swagger-ui.path=/swagger-ui.html
# # Configurações personalizadas
# app.custom.property=value
# app.another.custom.property=anotherValue
# # Fim do arquivo application.properties

